# PelizzAI SOURCE-REPO SENTINEL (source mode)
#
# The PRESENCE of this file is the ONLY criterion that marks this repository as
# the source repo of the PelizzAI harness. While it is present:
#   - the harness does NOT create the consumer `pelizzai/` runtime (state/specs/plans/profile);
#   - the pelizzai-writegate hook SKIPS Rule B (kickoff via consumer state);
#   - the pelizzai-session-start hook does NOT suggest consumer bootstrap;
#   - pelizzai-onboard treats the project as a source (scan without bootstrap).
#
# NEVER copy this file into a consumer project: it would disable the consumer
# lifecycle and the writegate enforcement there. The correct distribution is:
#   node scripts/sync-harness.mjs --export-consumer <project-path>
#   # or the wrappers sync-harness.ps1 / sync-harness.sh
# which copies core skills, hooks, and useful scripts, generates the consumer
# CLAUDE.md, and EXCLUDES this sentinel by contract (removing it from the
# target if it exists).
